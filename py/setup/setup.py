from setuptools import setup


setup(name='',
      version='0.1',
      packages=[],
      package_dir={},
      install_requires=['flake8',
                        'pytest'],
      python_requires='>=3.0')
